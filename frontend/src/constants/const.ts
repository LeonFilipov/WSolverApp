export enum LetterState {
    PERFECT = 'perfect',
    CORRECT = 'correct',
    ABSENT = 'absent',
    UNKNOWN = 'unknown',
}

export enum DataSetState {
    FILLED = 'filled',
    EMPTY = 'empty',
}

export enum PressedKey {
    BACKSPACE = 'Backspace',
    ENTER = 'Enter',
}
